version: '3.7'
services:
  poketbase: 
    image: pocketbase
    volumes:
      - type: volume
        source: 'data'
        target: '/pb_data'
    ports:
      - 8696:8096
    restart: unless-stopped
volumes: 
  data:
