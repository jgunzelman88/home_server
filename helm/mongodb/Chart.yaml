apiVersion: v2
name: mongodb
description: MongoDB on k3s with a self-hosted web build of the real MongoDB Compass UI (compass-web) at /mongo, logged into via Keycloak OIDC, behind Traefik ingress
type: application
version: 0.2.0
appVersion: "7"
