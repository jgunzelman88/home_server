apiVersion: v2
name: mongodb
description: MongoDB on k3s with a web-based admin UI (mongo-express, standing in for the desktop MongoDB Compass app) at /mongo, SSO-gated through Keycloak via an oauth2-proxy sidecar, behind Traefik ingress
type: application
version: 0.1.0
appVersion: "7"
