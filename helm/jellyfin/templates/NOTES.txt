Jellyfin has been deployed!

{{- if and .Values.ingress.enabled .Values.ingress.traefik.enabled }}

Access Jellyfin at:
  https://{{ .Values.ingress.traefik.host }}{{ .Values.ingress.traefik.path }}

Note: "{{ .Values.ingress.traefik.host }}" is a LAN hostname so Traefik will serve
a self-signed certificate. You may see a browser security warning — this is expected.
To suppress it either:
  1. Install the Traefik CA cert on your devices (find it in the traefik pod under
     /data/traefik.crt), or
  2. Add a certResolver in values.yaml if you have ACME/Let's Encrypt configured.

{{- else }}

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}{{ (index .paths 0).path }}
{{- end }}
{{- else if eq .Values.service.type "ClusterIP" }}
  kubectl port-forward svc/{{ include "jellyfin.fullname" . }} 8096:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Then visit: http://localhost:8096
{{- end }}

{{- end }}

Media volumes mounted at:
{{- range .Values.hostPathVolumes }}
  - {{ .mountPath }}  (host: {{ .hostPath }})
{{- end }}
